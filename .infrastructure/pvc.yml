apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-data
  namespace: todoapp
spec:
  volumeName: pv-data
  resources:
    requests:
      storage: 1Gi
  accessModes:
    - ReadWriteMany
